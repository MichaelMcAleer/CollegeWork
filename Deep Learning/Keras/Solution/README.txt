Both iPython Notebooks make reference to a path in my own gDrive account.

To change this to your own gDrive, you need only change the unzip path on line
7 in both notebooks:

Example:
!unzip "/gDrive/path/to/file/data1-2.h5.zip"
